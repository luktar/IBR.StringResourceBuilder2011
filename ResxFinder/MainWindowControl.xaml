<UserControl x:Class="ResxFinder.MainWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid>
        <DockPanel LastChildFill="True" Height="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <DockPanel DockPanel.Dock="Top" HorizontalAlignment="Stretch"
>
                <Button DockPanel.Dock="Left" Content="Properties" Command="{Binding PropertiesCommand}"/>
                <Button DockPanel.Dock="Right" Name="button" Content="Run!" Command="{Binding RunCommand}"/>

            </DockPanel>            
            <TextBox DockPanel.Dock="Top" Background="White"
                     TextWrapping="Wrap" Text="{Binding Text}" Width="Auto" Height="Auto"
                     HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>
        </DockPanel>
    </Grid>
</UserControl>
